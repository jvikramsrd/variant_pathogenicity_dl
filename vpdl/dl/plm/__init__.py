"""Protein language models: backbones, embeddings, zero-shot scores, PEFT, fine-tuning.

Heavy imports (torch, transformers) happen inside functions, so importing this
package costs nothing on a machine that only runs the tabular arms.
"""
